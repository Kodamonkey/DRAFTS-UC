[cosmic-ray]
module-path = "src/core/pipeline_parameters.py"
timeout = 30.0
excluded-modules = []
test-command = "python -m pytest tests/test_mut_fast.py -x -q"

[cosmic-ray.distributor]
name = "local"
