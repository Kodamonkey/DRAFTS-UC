# Direct Python dependencies for DRAFTS pipeline — pinned for reproducibility.
# Baseline: Python 3.11–3.14, captured from the working dev environment.
#
# For an EXACT reproducible environment (direct + transitive, with hashes), use:
#   pip install -r requirements.lock.txt
# This file lists only direct deps; the lock file is the source of truth for CI/deploys.
#
# Optional faster FITS I/O (prebuilt wheels for Python 3.10–3.12 on Windows):
#   pip install fitsio==1.2.6
# The pipeline falls back to Astropy when fitsio is absent (e.g. Python 3.14 on Windows).
#
# torch/torchvision: the pins below pull the default PyPI build (CPU, or CUDA-bundled
# depending on platform). For a specific CUDA build, install the SAME version numbers
# from the CUDA index, e.g.:
#   pip install torch==2.11.0 torchvision==0.26.0 --index-url https://download.pytorch.org/whl/cu121

astropy==7.2.0
opencv-python==4.13.0.92
matplotlib==3.10.8
numba==0.65.0
numpy==2.4.6
pandas==2.3.3
psutil==7.1.3
pyyaml==6.0.3
scipy==1.17.0
seaborn==0.13.2
scikit-image==0.26.0
scikit-learn==1.8.0
timm==1.0.26
torch==2.11.0
torchvision==0.26.0
tqdm==4.67.1
your==0.6.7
blimpy==2.0.31
jplephem==2.24
